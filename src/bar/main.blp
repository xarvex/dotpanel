using Gtk 4.0;

template $DotpanelBar: ApplicationWindow {
    styles [
        "bar",
    ]

    CenterBox {
        start-widget: Box {
            $DotpanelHostBarModule {}
        };

        center-widget: $DotpanelWorkspaceBarModule {};
    }
}
